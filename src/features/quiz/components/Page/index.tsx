import { FC } from 'react'
import { Quiz } from '../../store/quizSlice'

interface QuizPageProps {

}

const quizPageComponent: FC<QuizPageProps> = () => {
    return (
        <text>
            hello
        </text>
    )
}

export default quizPageComponent